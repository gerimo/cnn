<%= simple_form_for(@fb_token) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :token %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
